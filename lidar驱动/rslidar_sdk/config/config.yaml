common:
  msg_source: 2                                         #0: not use Lidar
                                                        #1: packet message comes from online Lidar
                                                        #2: packet message comes from ROS or ROS2
                                                        #3: packet message comes from Pcap file
                                                        #4: packet message comes from Protobuf-UDP
                                                        #5: point cloud comes from Protobuf-UDP
  send_packet_ros: true                                 #true: Send packets through ROS or ROS2(Used to record packet)
  send_point_cloud_ros: true                            #true: Send point cloud through ROS or ROS2
  send_packet_proto: false                              #true: Send packets through Protobuf-UDP
  send_point_cloud_proto: false                         #true: Send point cloud through Protobuf-UDP
  pcap_path: /home/robosense/lidar.pcap                 #The path of pcap file

#yellow
lidar:
  - driver:
      lidar_type: RS128            #LiDAR type - RS16, RS32, RSBP, RS128, RS80, RSM1, RSHELIOS
      frame_id: ars548           #Frame id of message
      msop_port: 6699              #Msop port of lidar
      difop_port: 7788             #Difop port of lidar
      start_angle: 0               #Start angle of point cloud
      end_angle: 360               #End angle of point cloud 
      min_distance: 0.2            #Minimum distance of point cloud
      max_distance: 200            #Maximum distance of point cloud
      use_lidar_clock: false       #True--Use the lidar clock as the message timestamp
                                   #False-- Use the system clock as the timestamp  
                                   
      split_frame_mode: 1	      
      cut_angle: 180
      x: 0
      y: 0
      z: 0
#      roll: 0
#      pitch: 0
#      yaw: 0
      roll: 0.00436332
      pitch: -0.0209438
      yaw: -0.00436315   
      
    ros:
      ros_recv_packet_topic: /sensor/RS128Packets        #Topic used to receive lidar packets from ROS
      ros_send_packet_topic: /sensor/RS128Packets          #Topic used to send lidar packets through ROS
      ros_send_point_cloud_topic: /sensor/RS128Points      #Topic used to send point cloud through ROS

       
   
     
    
